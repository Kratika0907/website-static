
name: 'Checks for unique name, id and social link'
'on':
  push:
    branches:
      - kratika
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps :
      - name: Checkout
        uses: actions/checkout@v1
      - name: run file 
        uses: ./